# API key required to call POST /api/generate
# Generate a strong random value (e.g. `openssl rand -hex 32`)
GENERATOR_API_KEY=replace-with-a-strong-random-secret

# Optional. Origin allowed to call the API from a browser. Defaults to "*".
# Example: https://your-other-app.com
ALLOWED_ORIGIN=*
